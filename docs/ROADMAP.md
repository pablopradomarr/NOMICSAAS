# ROADMAP — épicas y orden de ejecución

Estado: `PENDIENTE` · `DISEÑADA` (existe `docs/design/`) · `EN CURSO` · `CERRADA`. Cada épica se planifica con `/epica E<n>` y se ejecuta con `/sprint E<n>`. Nivel 2 = ADR firmado antes de codificar.

| Épica | Alcance | Depende de | Nivel | Estado |
|---|---|---|---|---|
| **E0 Base** | Sistema agéntico + docs, `lib/money.ts` + tests, hook de pureza, `runs/registro.jsonl`, CI (`.github/workflows/ci.yml`: lint, tsc, unit, integración, RLS como app_runtime, guard de pureza), `test:integration`/`test:integration:rls`/`test:all`. Pendiente menor: `test:e2e` Playwright, Supabase branch | — | 1 | CERRADA (salvo e2e/Supabase, se completan en E2) |
| **E1 Organizaciones y roles** | `Organization`, `Membership`, `Role`, `requireOrg`/`withOrg`, `tenantDb`+`tenantTransaction`, switcher, ajustes de organización, miembros, invitaciones, migración TaxHacker (user → org), ficheros por organización, RLS efectiva con `app_runtime` (escape hasta E3, ADR-0007), tenant leak + RLS tests | E0 | 2 (ADR-0002, 0007) | **CERRADA** 2026-09-04 (revisión APROBADA tras 3 rondas; deuda E3 en docs/ESTADO.md) |
| **E2 Plan de cuentas e impuestos** | `LedgerAccount` (NPGC GENERAL 910 / PYMES 798), `OrganizationAccountMap` (57 claves), `TaxRate` (28 tipos 2026, vigencias EXCLUDE), `AuditLog` append-only, árbol editable, import CSV, mapa, impuestos, auditoría de cambios, e2e Playwright | E1 | 2 (ADR-0008) | **CERRADA** 2026-09-04 (revisión APROBADA; pendientes menores → E3: importCustomPlan por createMany, alta de org + siembra en una unidad, virtualizar árbol) |
| **E3 Libro diario** | `FiscalYear`, `PeriodLock`, `JournalEntry/Line` (céntimos, triggers diferidos Σdebe=Σhaber, ≥1 debe/haber, anti contra-contra-asiento, GRANT de columna), motor puro `lib/ledger` (post/void/hash/tax/28 plantillas/invariantes I1, I7–I10, I-E3-1..7/informes diario-mayor-sumas y saldos con provenance), numeración FOR UPDATE, idempotencyKey, cierre de ejercicio atómico, UI (diario, asiento manual/plantilla, mayor, sumas y saldos con sello, ejercicios), fixtures v1.1 byte-idénticos, RLS estricta sin escapes (ADR-0009, FORCE en 20 tablas, app_maintenance) | E2 | 2 (ADR-0003, 0009) | **CERRADA** 2026-09-05 (auditor CONFORME; revisión APROBADA tras 2 rondas; deuda menor en ESTADO) |
| **E4 Analítica base** | `BusinessLine`, `Project` extendido, `CostCenter` (8 kinds, marginLevel), `MarginLevelConfig` con vigencias, dimensiones en líneas con FK/CHECK xor/trigger LN, hashes v2 (ADR-0011: ledgerHash sin analítica, entryHash total, analyticsHash), motor `lib/analytics` (matriz INGRESOS→RESULTADO byte a byte con la esperada), I4 + I-E4-1..12, reclasificación analítica auditada (ADR-0010), UI PyG analítica/proyectos/CECOs/LN/config, e2e | E3 | 2 (ADR-0004, 0010, 0011) | **CERRADA** 2026-09-05 (auditor CONFORME Δ=0 en 10 cifras; revisión APROBADA; deuda menor en ESTADO) |
| **E5 Liquidación de CECOs** | `AllocationRule/Target/Run/Line` versionadas con vigencias, drivers (FIXED_PERCENT, REVENUE_SHARE sin 74x, DIRECT_COST_SHARE, EQUAL, MANUAL), cascada DAG, Hamilton entero (BigInt) con desempate por código, runs inmutables sellados (linesHash) con reversión/supersede, I5 + I-E5-1..12 en producción, PyG analítica imputada con cuarto sello (`allocationRunSetHash`) y «pendiente de liquidar», grafo de cascada, UI de reglas/simulación/sellado | E4 | 2 (ADR-0004, **0013**) | **CERRADA** 2026-09-06 (auditor CONFORME: 14 líneas, I5.a/b, I4 por nivel y EBITDA por proyecto reconstruidos con Δ=0, error inyectado detectado por linesHash/I-E5-12; revisión APROBADA tras 3 rondas; QA 10 escenarios adversariales; deuda fechada en ESTADO) |
| **E6 Informes financieros** | Balance (4 fotos × 2 modelos), PyG contable (normal/PYMES), cashflow directo por 7 buckets + indirecto + EFE, aging, dashboard sobre el diario, `ReportRun` inmutable con caché por (paramsHash, ledgerHash, analyticsKey, planHash, accountMapHash, gitSha), umbrales EV-* con sello, ManualReviewFlag, export CSV/XLSX/PDF con provenance, I2/I3/I6 + I-E6-1..20; `cashflowBucket` en seed; perf: una transacción por petición | E3, E4 | 2 (ADR-0012) | **CERRADA** 2026-09-06 (auditor CONFORME: balance/PyG/cashflow reconstruidos por SQL con Δ=0; revisión APROBADA tras 2 rondas + perf; deuda menor en ESTADO) |
| **E7 Auditoría** | Pestaña `/audit` (siete familias de checks con evidencia y drill-down ≤ 3 clics, **cuadres de cierre**, calidad de datos de E8, `AuditLog`, historial de `InvariantRun` con diff de estados y de cifras, forzar revisión, barrido del almacén), `InvariantRun` persistente y sellado, **conciliación bancaria** (`BankAccount`/`BankStatement`/`BankStatementLine`/`BankMatchGroup` N-a-M, CSV y Norma 43, sugerencias deterministas sin LLM, propuesta de asiento para comisiones), motor puro `lib/audit` con **I-E7-1…17**, badge `validado contra fuente`, test de error inyectado desde la UI. **Cierra además** la deuda de E5/E6/E8: runs con `lines_hash` NULL, `journal_lines` a `bigint`, `ReportRun` que no caduca al alterar `allocation_lines` (I-E7-10), barrido completo del almacén para I-E8-2, **split N-a-1 en la interfaz**, unificación de `CASHFLOW` en `ReportType`, RLS en `users` y perf test por cargador | E6, E8 | 2 (**ADR-0015 APROBADO** 2026-09-07) | **CERRADA** 2026-09-07 · **auditor CONFORME** (ronda 2; en la 0 DISCREPANCIA con siete hallazgos —tres ALTA: el cuadre en divisa mezclaba monedas, el `headline` se firmaba en cero a 31-12 y I-E7-12 no podía medir nada—; en la 1, DISCREPANCIA por **N-1**, que el propio arreglo de H-3 introdujo: reconocer la diferencia de cambio en 768/668 la restaba dos veces y dejaba la cuenta en `REQUIERE REVISIÓN` para siempre) · **revisor APROBADO** (ronda 2; en la 1 CAMBIOS REQUERIDOS: documentación de ADR-0015 sin escribir, «explicado» sin conectar y las dos pantallas nuevas sin test de rendimiento) · QA: BUG-E7-1 (`--reset-org` no limpiaba las tablas de E7 y rompía la suite e2e) y BUG-E7-2 (sin medición por cargador) cerrados con test propio. Tests: unit 1 552 ✓ · integración 2 042 ✓ · RLS 183 ✓ · e2e 35 ✓ · build OK · los cinco techos de §8 medidos con volumen real (5 000 líneas, 5 000 apuntes, 100 000 `audit_logs`). Deuda fechada en `docs/ESTADO.md` §«E7 — deuda y decisiones» |
| **E8 Documentos → asientos** | `ExtractionRun` + `PromptVersion` append-only, `Counterparty`, `reconcile()` RC-01…RC-25, `postFromProposal` → asiento con la cuota del documento y bloques de pasivo por naturaleza de línea, `Transaction.status` con `POSTED ⟺ asiento` y anular-y-rehacer, `files.sha256 NOT NULL` (y comparado con el almacén), `cachedParseResult` eliminada, `ExchangeRate` en servidor (tasa del `documentDate`, BCE, residuo de conversión cero), facturas emitidas con `InvoiceSeries`, los cuatro badges de confianza por campo, bandeja + revisión + lote + configuración documental, I-E8-1…20 (tres puentes al 303 y puente documento↔asiento), cierre de G-01…G-04 y G-07…G-22 | E3, E2 | 2 (ADR-0005, **0014 APROBADO** 2026-09-06) | **CERRADA** 2026-09-06 · **auditor CONFORME** (ronda 2; en la 1 dio DISCREPANCIA por los tres puentes al 303 mal derivados: H-1 divisa y H-2 rectificativa) · **revisor APROBADO** (ronda 2; en la 1 BLOQUEADO por deuda sin fechar) · QA: BUG-E8-1 y BUG-E8-2 cerrados con test propio. Tests: unit 1 331 ✓ · integración 1 763 ✓ · RLS 155 ✓ · e2e 28 ✓ · build OK · fixture `extraccion-esperada.v1.1.json` reproducible byte a byte. Los quince casos se replican además **sobre el NPGC PYMES real sembrado**, sin traducciones de arnés. Deuda fechada en `docs/ESTADO.md` §«E8 — deuda y decisiones» |
| **E9 Cierre y recurrentes** | `RecurringEntry`/`RecurringOccurrence` con idempotencia por índice único `(regla, periodo)`, `FixedAsset` + cuadro determinista derivado (lineal, residuo a la última cuota, revisión prospectiva, baja y venta, atribución por activo con `JournalLine.fixedAssetId`), `Accrual` 480/485/567/568, `DebtSchedule` con desglose de vencimientos, liquidación de IVA desde el libro registro de E8 (prorrata definitiva con su regularización 634/639, guardia bloqueante de bienes de inversión, RECC/REDEME fechados con 4728/4778 y cobro parcial, DUA en sus dos modalidades, **mapa completo del 303** como vista derivada, retenciones 111/115/123), ajustes de cierre (valor actual del aplazamiento **como valoración inicial**, diferencias de cambio 668/768 sobre partidas **monetarias** a tasa de cierre efectiva, reclasificación corriente/no corriente medida desde el cierre), cierre completo en el orden corregido con `ClosingRun` sellado y **checklist de 41 pasos / 9 bloqueantes**, **reapertura controlada** (revierte también T-25), **distribución del resultado** 129 → 112/113/120/526/557, bloqueo de periodos B-6/B-7/B-8, motores puros `lib/closing/` y `lib/recurring/`, **I-E9-1…26**, nueve plantillas nuevas (catálogo 28 → **37**), UI `/ledger/{closing,recurring}`, `/settings/{assets,debt,periods}`, `/reports/vat`. **Cierra la deuda fechada de E7/E8**: 523→173, valor actual, RECC/REDEME, DUA, 668/768, retención y archivado de runs, rate limit de la cola de extracción | E3, E7, E8 | **2 (ADR-0016 APROBADO 2026-09-07, D1–D12)** | **CERRADA** 2026-09-14 · **auditor CONFORME** (ronda 3; en la 0 DISCREPANCIA con H-1…H-6 —tres bloqueantes: el signo de T-32 invertido, los 27 `I-E9-*` que no corrían nunca y la reapertura imposible por CA-1—; en la 1, DISCREPANCIA por **R-1** (los contra-asientos de la reapertura fuera del ejercicio: el recierre marcaba `CLOSED` sin postear T-26/T-27/T-28) y **R-2** (los 22 pares sólo existían por el backfill, e I-E9-16 pasaba por vacuidad); en la 2, CONFORME con un menor —el impuesto no quedaba pendiente al reabrir— cerrado con guardia y test) · **revisor APROBADO** (ronda 2; en la 1, 1 BLOQUEA —el paso 12 de O-17 no existía— y 8 DEBE) · QA: BUG-E9-1 (`reviseAssetAction` admitía revisiones retroactivas) cerrado con test propio. Tests: unit 1 968 ✓ · integración 2 551 ✓ · RLS 185 ✓ · e2e `cierre` 8/8 y `recurrentes-iva` 9/9 ✓ · build OK · los ocho techos de rendimiento activos. **Checklist de 43 pasos / 9 bloqueantes** (el diseño enumera 41 en la cabecera y 43 en el detalle; manda el detalle) y **22 pares** de reclasificación (R2-1 hablaba de 23: 527/528 quedan fuera). Deuda fechada en `docs/ESTADO.md`. Diseño: `docs/design/E9-cierre-recurrentes.md` (**628 h / 26 tareas**). Validación contable (`E9-validacion-cierre.md`): **CONFORME CON OBSERVACIONES** — en la ronda 0 dio **NO CONFORME** en D4–D8 y en el alcance, con **22 bloqueantes** (O-1…O-22) y **ocho** (O-23…O-30) más las respuestas a Q-1…Q-14; las **30 están incorporadas**, D4–D8 reescritas, D9–D12 añadidas, y los tres retoques de la re-validación **R2-1** (23 pares: 176↔5595, 177↔500, cuatro de partes vinculadas sin 514, 527/528 fuera), **R2-2** (capital social derivado del saldo de `100`) y **R2-3** (`LedgerAccount.isMonetary`). **ADR-0016 APROBADO** por Pablo (permiso delegado de 2026-09-04): lista para **`/sprint E9`** en tres olas paralelas (A/B/C) |
| **E10 Presupuesto y horas** | `Budget` versionado y sellado (`budgetHash` con el `marginLevel` congelado, vigencias sin solape **ni hueco**, `partialFrom`, signo forzado por tipo analítico, O-A6 cerrada), presupuesto vs real **en el mismo estado de imputación** con forecast derivado y umbrales EV-11…EV-13/EV-15…EV-17, `TimeEntry` (**minutos enteros**, aprobación, contra-apunte), `Employee`/`EmployeeRate` con vigencias y `basis`, `HeadcountSnapshot`, drivers **HOURS y HEADCOUNT** con su cuarto sello (`timeHash` **+ ventana efectiva**) y base FTE·mes, motores puros `lib/budget/` y `lib/time/`, **I-E10-1…18**. Cierra además la deuda de E5/E9: `MIXED`/`DRAFT` retirados, fixture `ejercicio-completo-v2` reversionado, N+1 de la staleness en lote, activos con destino analítico | E5 | **2 (ADR-0018 APROBADO 2026-09-14, D1–D6, más D7 el 2026-09-15)** | **CERRADA** 2026-09-15 · **auditor CONFORME** (ronda 3; en la 0 **DISCREPANCIA** con H-1…H-7 —dos GRAVES: los dieciocho `I-E10-*` que nadie ejecutaba y el `budgetHash` irreproducible en cuanto se relevaba una versión—; en la re-auditoría de la 1, DISCREPANCIA por cuatro puntos —la regresión de `I-E10-12` con el ejercicio regularizado, el bloque de absorción del fixture que contradecía al motor, la provenance por celda que devolvía 0 filas en los niveles acumulados y la inyección (b) sin run de actividad—; en la 3, un solo punto accionable: `I-E10-12` contaba como personal **todo** lo repartido por un driver `HOURS`) · **revisor APROBADO** (ronda 2; en la 1, 3 BLOQUEA —los nueve techos de §9 sin medir, tres deudas del registro fuera de `ESTADO.md` y el `budgetHash` sin las horas— y 5 DEBE) · QA: BUG-E10-1 (`--reset-org` no vaciaba presupuesto ni horas) y BUG-E10-2 (un parte APROBADO no se podía borrar ni como operador; **ADR-0018 D7**, GUC verificado) cerrados con test propio. Tests: unit 2 250 ✓ · integración 2 967 ✓ · RLS 211 ✓ · e2e `presupuesto` 10/10 y `horas-presupuesto-real` 6/6 ✓ · build OK · **los nueve techos de §9 activos**. Fixture sellado reversionado dos veces en la corrección: **v1.2** (v1.0 y v1.1 congelados). Deuda fechada en `docs/ESTADO.md`. Diseño: `docs/design/E10-presupuesto-horas.md` (**578 h / 23 tareas**). Validación de control de gestión `E10-validacion-controlling.md`: **CONFORME CON OBSERVACIONES** (5 bloqueantes, O-E10-0…22, Q-1…Q-7 resueltas, I-E10-14…18) — **las 22 incorporadas**, más **O-E10-23** (`analytic_type` obligatorio) |
| **E11 Plataforma SaaS** | Stripe por organización, backups por organización de todas las tablas, límites por plan, onboarding | E1 | 1 | PENDIENTE |
| **E12 Fiabilidad DoD** | README-FIABILIDAD.md, tests de aceptación C1–C7 end-to-end, test memoria borrada, propuesta v1.1 | E7, E8 | 1 | PENDIENTE · **`README-FIABILIDAD.md` adelantado en el cierre de E7** (T22): qué garantiza el sistema, los invariantes por familia, los sellos y cómo auditarlo. E12 lo completa con los tests de aceptación C1–C7 |
| **E13 Autenticación** | Login con email + contraseña (better-auth `emailAndPassword`, hash scrypt, política de contraseña), pantalla `/enter` con identidad CFOnomic, alta por invitación con contraseña, recuperación de contraseña por email (Resend), `scripts/create-admin.ts` para el primer ADMIN, restablecer contraseña desde `/settings/members`, `SELF_HOSTED_MODE=false` en preview; e2e de login/roles. Fuera: 2FA, SSO, Supabase Auth | E1 | 1 (sin motor ni RLS; ADR informativo) | **CERRADA** 2026-09-07 en rama `feat/e13-auth` (revisión CAMBIOS REQUERIDOS → corregidos: migración `20260919090000`, cortocircuito rate limit; QA PASS 13/13 e2e auth + 1007 unit + 1321 integración; BUG-E13-1 `Account.id` sin default corregido; ADR-0017 ACEPTADO). Pendiente de fusión en `main` y de pasar el preview a `SELF_HOSTED_MODE=false` (runbook `docs/deploy/create-admin.md`) |

## Orden sugerido de sprints
S1: E0 + E1 · S2: E2 + E3 · S3: E4 + E6 (balance/PyG) · S4: E5 + E6 (analítica, cashflow) · **S5: E8 ✅** · **S6: E7 ✅ + E9 ✅** · **S7: E10 ✅** + E11 · S8: E12.

Hechas: **E0, E1, E2, E3, E4, E5, E6, E7, E8, E9, E10, E13**. Siguiente: **`/epica E11`**. E10 cerró además la deuda que E5 y E9 dejaron abierta: O-A6 (cuatro índices únicos parciales sobre `budget_lines`), el N+1 de la staleness en lote —tres consultas sea cual sea N—, los enums `MIXED`/`DRAFT` y el fixture `ejercicio-completo-v2`, que ya carga (schemaVersion 3.1). E9 cerró además la deuda que E7 dejó abierta: la diferencia de cambio que allí se **medía** (I-E7-12, NRV 11ª.2.2) aquí se **reconoce** en 668/768.

## Gaps ALTA de TaxHacker (docs/AUDITORIA-FIABILIDAD.md) → épica que los cierra
G-01 Σitems≠total sin validar y unidades mezcladas → **E8 ✅** · G-02 extracción parcial sin marcar → **E8 ✅** · G-03 `cachedParseResult` memoria de cifras → **E8 ✅** · G-04 tasa de cambio en navegador sin persistir → **E8 ✅** · G-05 `profitPerCurrency` NaN → **E6 ✅** · G-06 series temporales a 0 sin aviso → **E6 ✅**. Los seis ALTA están cerrados.
