# Revisión E1 — organizaciones y roles (`git diff 83cdf86...HEAD`) · revisor-codigo, contexto limpio, 2026-09-04

Estado: **RESUELTO en el sprint E1-fix** (2026-09-04) — 3 BLOQUEA + 15 DEBE cerrados; PUEDE #19, #20, #21, #24, #25, #26 cerrados; #22 y #23 diferidos con motivo. Ver columna «Resolución».

Veredicto original: **CAMBIOS REQUERIDOS** (3 BLOQUEA · 15 DEBE · 8 PUEDE). QA: PASS condicionado (tests adversariales añadidos en tests/integration/e1-*.test.ts; CA-1 migración sin test).

| # | Fichero:línea | Sev. | Problema | Sugerencia | Resolución (sprint **E1-fix**, 2026-09-04) |
|---|---|---|---|---|---|
| 1 | lib/db.ts:269-280 · prisma/migrations/20260904120300_e1_rls/migration.sql:29-38 | BLOQUEA | `tenantTransaction` (único punto que ejecuta `SET LOCAL app.current_org`) no se invoca en producción; sin FORCE RLS; `.env.example`/docker-compose no usan `app_runtime` → barrera 2 inerte. ADR-0007 exige app_runtime + SET LOCAL desde E1. | Cablear app_runtime + FORCE RLS + envolver escrituras en tenantTransaction, o ADR nuevo con aprobación. | **HECHO** · `prisma/migrations/20260904140000_e1_rls_effective/migration.sql` (FORCE RLS + `app.current_user()`), `lib/db.ts` (la extensión envuelve TODA operación en `$transaction` con `set_config` de ambos GUC; `tenantTransaction(orgId, userId?, fn)`; `withTenantGucs`), `.env.example`, `docker-compose*.yml` y `prisma.config.ts` (`DATABASE_URL`=`app_runtime` / `DIRECT_URL`=owner). Humo: `tests/integration/e1-rls-app-runtime.test.ts` |
| 2 | migration.sql:48-59 · models/organizations.ts:47 | BLOQUEA | Con app_runtime la app no arranca: WITH CHECK sin escape y GUC nunca fijado; crear `organizations` exige id = app.current_org() (insatisfacible). | Políticas de organizations/memberships por user_id; SET LOCAL en createOrganizationWithOwner; test de humo con app_runtime. | **HECHO** · políticas de `organizations`/`memberships` por pertenencia (`app.current_user()`) en la migración `…_e1_rls_effective`; INSERT de `organizations` permitido con usuario identificado; `models/organizations.ts::createOrganizationWithOwner` y `ensurePersonalOrganization` en transacción con GUC de usuario |
| 3 | lib/files.ts:13-15,39-42 · app/(app)/files/download/[fileId]/route.ts:132 · app/(app)/export/transactions/route.ts:250,341 | BLOQUEA | Ruta física derivada del email del solicitante: otro miembro de la org recibe 404. | uploads/<organizationId>/… con migración de disco, o resolver por file.uploadedById. | **HECHO** · `lib/files.ts` (`getOrganizationUploadsDirectory`, `fullPathForFile(org,…)`, `getStaticDirectory(org)`), `lib/uploads.ts`, rutas de `files/`, `export/`, `transactions/`, `unsorted/`, `apps/invoices/`; migración de disco en `scripts/migrate-uploads-to-org.ts` (dry-run por defecto) |
| 4 | app/(app)/settings/backups/data/route.ts:405 | DEBE | Backup solo empaqueta el directorio del ADMIN que lo lanza. | Recorrer todos los miembros o directorio de org. | **HECHO** · `app/(app)/settings/backups/data/route.ts` y `…/actions.ts` empaquetan y restauran `uploads/<organizationId>/` |
| 5 | app/(app)/files/static/[filename]/route.ts:167 | DEBE | Logo de facturación servido desde directorio del usuario: 404 para otros miembros. | Igual que 3. | **HECHO** · `app/(app)/files/static/[filename]/route.ts` + `lib/uploads.ts::uploadStaticImage` usan `getStaticDirectory(org)` |
| 6 | lib/files.ts:118-126 | DEBE | Cuota suma directorios de todos los emails de miembros → cruce entre orgs. | Medir por organizationId. | **HECHO** · `lib/files.ts::getOrganizationStorageUsed(org)` mide el directorio de la organización; `lib/uploads.ts::syncOrganizationStorage` ya no suma emails de miembros |
| 7 | app/(app)/settings/members/page.tsx:23 | DEBE | requireOrg("VIEWER"): cualquier miembro ve lista y emails de invitaciones. | requireOrg("ADMIN") + notFound(). | **HECHO** · `app/(app)/settings/members/page.tsx`: `requireOrg(Role.ADMIN)` + `notFound()` al capturar `AuthzError` |
| 8 | app/(app)/settings/members/page.tsx:33-39 | DEBE | RSC escribe en BD (markInvitationExpired) con rol VIEWER. | Mover a job/action; filtrar en memoria. | **HECHO** · `models/invitations.ts::listLiveInvitations(db, now)` filtra por `expiresAt` en lectura; el marcado `EXPIRED` vive sólo en `acceptInvitationAction` |
| 9 | app/(app)/layout.tsx:36 · lib/authz.ts:69 | DEBE | AuthzError("NO_ORGANIZATION") no capturado; usuario sin membresía → 500 y no alcanza /organizations/new. | redirect a /organizations/new o sacar /organizations/** del layout. | **HECHO** · `/organizations/new` movida al route group `app/(onboarding)/` (layout propio sin `requireOrg`); `app/(app)/layout.tsx` captura `NO_ORGANIZATION` → `redirect("/organizations/new")`; `withOrg` hace lo mismo en actions |
| 10 | package.json:15 · vitest.config.ts:9 | DEBE | `npm run test` excluye tests/integration; skipIf sin DATABASE_URL_TEST → leak test no corre en CI. | test:integration en pipeline; fallar si falta la variable. | **HECHO** · `package.json`: `test:all` = unit + integration; `vitest.integration.setup.ts` lanza si falta `DATABASE_URL_TEST` con `CI=true`; `.github/workflows/ci.yml` con job `test-integration` (servicio postgres:17) y `pureza-motor` |
| 11 | tests/integration/ | DEBE | Tests QA sin versionar (ya corregido en commit siguiente); falta migration.test.ts con fixture TaxHacker. | Versionar + fixture tests/fixtures/taxhacker-pre-e1.sql. | **HECHO** · `tests/fixtures/taxhacker-pre-e1.sql` (dump pre-E1: 2 usuarios, `code` solapado, 1 transacción cada uno, settings/fields/currencies) y `tests/integration/migration.test.ts` (BD temporal → fixture → `migrate deploy` → 0 huérfanos, 2 orgs, 2 memberships ADMIN, uniques compuestos, RLS forzada) |
| 12 | 20260904120200_e1_enforce/migration.sql:46-51 | DEBE | Seis DROP INDEX sin IF EXISTS. | DROP INDEX IF EXISTS. | **ANOTADO** · la migración `20260904120200_e1_enforce` YA ESTÁ APLICADA en `erp`/`erp_test`; editarla rompería su checksum y `prisma migrate deploy` (CLAUDE.md). Se deja intacta y se anota en `E1-organizaciones-roles.md` §Desviaciones aceptadas. `migration.test.ts` demuestra que la cadena completa se aplica sobre un dump limpio |
| 13 | 20260904120100_e1_backfill/migration.sql:14-22 | DEBE | Slug email+6 hex; colisión en organizations_slug_key aborta backfill. | uuid completo o ON CONFLICT DO NOTHING. | **PARCIAL** · `models/organizations.ts::buildOrganizationSlug` usa ya el uuid COMPLETO (colisión imposible en runtime). El SQL de backfill `20260904120100` queda intacto por el mismo motivo que #12, anotado como desviación |
| 14 | models/users.ts:75-83 · app/(auth)/invite/[token]/actions.ts:25-41 | DEBE | D-3: alta sin verificación de email ni rate limit. | Rate limit por IP/token; cuenta utilizable solo tras OTP. | **HECHO** · `lib/rate-limit.ts` (+ `lib/rate-limit.test.ts`): límite en memoria por IP y por hash de token; `Invitation.attempts` (migración `…_e1_fix_schema`) se incrementa en cada intento fallido y bloquea a 5; `models/users.ts::getOrCreateInvitedUser` crea la cuenta con `emailVerified=false` |
| 15 | app/(auth)/invite/[token]/actions.ts:85-96 | DEBE | Aceptación en 3 pasos sin transacción. | $transaction + unique de memberships. | **HECHO** · `models/invitations.ts::acceptInvitation` hace membresía + marca de aceptada en UN `$transaction`, apoyándose en el unique `(organization_id, user_id)` (P2002 ⇒ idempotente) |
| 16 | app/api/stripe/webhook/route.ts:29 · models/organizations.ts:38 | DEBE | console.log del evento Stripe íntegro; stripe_customer_id no único → findFirst arbitrario. | Log id/type; unique parcial. | **HECHO** · `app/api/stripe/webhook/route.ts` loguea sólo `event.id`/`event.type`; unique `organizations_stripe_customer_id_key` (NULLs distintos ⇒ parcial de facto) en `…_e1_fix_schema`; `getOrganizationByStripeCustomerId` → `findUnique` y `…OrThrow` en el webhook |
| 17 | lib/db.ts:10 | DEBE | log: ["query",...] imprime SQL con parámetros en producción. | Por entorno. | **HECHO** · `lib/db.ts::prismaLogLevels()`: `query` sólo en desarrollo; producción `warn`/`error` |
| 18 | app/(app)/files/actions.ts:12-44 | DEBE | Sin validación de mimetype/tamaño por fichero; file.type del cliente. | Lista blanca + límite + detección por buffer. | **HECHO** · `lib/uploads.ts`: `ALLOWED_UPLOAD_TYPES` (pdf, png, jpg, jpeg, webp, heic, csv, xlsx, txt, eml), `MAX_UPLOAD_FILE_SIZE` = 25 MB y `sniffFileExtension()` por magic bytes (sin dependencia nueva); `uploadFilesAction` devuelve el motivo. Tests en `lib/uploads.test.ts` |
| 19 | lib/db.ts:156-227 | PUEDE | Extensión no cubre include anidados ni $queryRaw (hoy tapado por FK compuestas). | Documentar + test. | **HECHO** · JSDoc de `tenantDb()` con los tres límites (relaciones anidadas, SQL crudo, modelos fuera de `TENANT_MODELS`) + dos tests nuevos en `lib/db.test.ts` |
| 20 | eslint.config.mjs:48-63 | PUEDE | Regla no cubre lib/** ni `import * as`. | Añadir lib/** y patterns. | **HECHO** · `eslint.config.mjs` cubre `lib/**` y `forms/**` y añade `patterns` con `importNamePattern` (cierra `import * as`) |
| 21 | migration.sql:41-59 (RLS) | PUEDE | Bajo RLS estricta el switcher (getUserMemberships) es imposible. | Política adicional user_id = app.current_user(). | **HECHO** · política `tenant_isolation` de `memberships` con `user_id = app.current_user()`; verificado en `e1-rls-app-runtime.test.ts` («sólo se ven las organizaciones y membresías de A») |
| 22 | models/defaults.ts:39-45 · models/currencies.ts:10 | PUEDE | Monedas duplicadas (org + catálogo global). | Sembrar solo las que falten o quitar lectura híbrida. | **NO ABORDADO** · monedas duplicadas (org + catálogo global). Queda para E2 junto al plan de cuentas; no hay riesgo de aislamiento |
| 23 | lib/authz-core.ts:47-49 · lib/authz.ts:93 | PUEDE | Cookie sin iat/versión, 365 días. | Incluir iat y rechazar por antigüedad. | **NO ABORDADO** · `iat`/versión en la cookie de organización activa. La cookie no es credencial (`requireOrg` revalida la membresía en cada request), así que el riesgo es de comodidad, no de autorización. Diferido |
| 24 | lib/authz.ts:66-75 | PUEDE | AuthzError no capturado → excepción genérica. | ActionState{success:false} / notFound(). | **HECHO** · `lib/authz.ts::withOrg(minRole, fn)` traduce `AuthzError` → `ActionState{success:false,error:"Sin permiso"}` (y `NO_ORGANIZATION` → redirect); usado en las cinco actions de `settings/members/actions.ts` |
| 25 | app/(app)/settings/members/actions.ts:167,197 · forms/memberships.ts:89,95 | PUEDE | Contrato por userId vs diseño por membershipId. | Alinear y anotar. | **ANOTADO** · desviación userId vs membershipId documentada en `docs/design/E1-organizaciones-roles.md` §Desviaciones aceptadas |
| 26 | docs/design/E1-organizaciones-roles.md:694 | PUEDE | leaveOrganizationAction no implementada. | Implementar o marcar diferida. | **HECHO** · `leaveOrganizationAction()` en `app/(app)/settings/members/actions.ts` (conserva el invariante «al menos un ADMIN» y limpia la cookie) |

Sólido: tenantDb compone con AND, aplana uniques compuestos, findUnique→findFirst, lanza en escritura cruzada; ESLint dispara; uniques/FK compuestas (I10 garantizado por motor); backfill idempotente; token randomBytes(32)+sha256; cookie timingSafeEqual; matriz de roles correcta en 70 llamadas salvo #7.
