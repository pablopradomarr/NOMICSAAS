# E7 — Revisión de código (`revisor-codigo`)

**Diff:** `git diff 0ff2a77...HEAD` (86c282d · 76d84c1 · c8ca5b3 · 34f2802) · **Fecha:** 2026-09-07
**Suites:** `lint` 0 errores (12 avisos heredados) · `test` **1 539 ✓ / 11 skip** · `test:integration` **2 008 ✓ / 1 fallo flaky** · `test:integration:rls` **183 ✓** · `build` **OK** · e2e no ejecutado (por encargo).

## Hallazgos

| # | Fichero:línea | Severidad | Problema | Sugerencia |
|---|---|---|---|---|
| 1 | `docs/MODELO-DATOS.md:206` · `.claude/skills/fiabilidad/SKILL.md` · `docs/ARQUITECTURA.md` | **DEBE** | El diff **no toca un solo documento** (`git diff --stat -- docs/` vacío). ADR-0015 §Consecuencias obliga a actualizar los tres: tipos `bigint` del diario, `ReportType.CASHFLOW`, el bloque E7 con las seis tablas, la regla del badge P6, los cuatro motivos de sello nuevos e **I-E7-1…17**. `MODELO-DATOS.md:206` sigue declarando `CASHFLOW_DIRECTO CASHFLOW_INDIRECTO` y ni SKILL ni MODELO-DATOS contienen la cadena `I-E7` (0 ocurrencias). CLAUDE.md §Principio 4 exige que los invariantes se definan **una sola vez** en `SKILL.md`; hoy los diecisiete viven sólo en `lib/audit/invariants-e7.ts`. | Cerrar antes del fin de E7 y anotarlo en `docs/ESTADO.md` con épica y fecha: es deuda real que ningún `runs/registro.jsonl` de las cuatro olas declara («Ninguna sin fecha»). |
| 2 | `app/(app)/audit/bank/shared.ts:34` · `lib/audit/confidence.ts:86,119` | **DEBE** | `resolvedLaterIds` **no se puebla en ningún sitio de producción**: la vista pasa `new Set()` y el único `badgeForFigure` real no pasa el campo opcional. Los criterios 1 y 2 de «explicado» de ADR-0015 D2/O-17 —«lo recoge un asiento posterior ya conciliado» / «una línea de extracto posterior ya conciliada»— son código muerto; sólo funciona el tercero (tránsito tipado). | Componer el conjunto en `models/bank.ts` (los pendientes que ya tienen contrapartida posterior conciliada) y pasarlo a los dos. El sesgo es seguro (nunca concede el badge de más), pero la pantalla llama «sin explicar» a pendientes que sí lo están y el motivo que muestra es falso. |
| 3 | `tests/integration/perf-pages.test.ts` | **DEBE** | El fichero de rendimiento **no se ha extendido**: `/audit` y `/audit/bank` no tienen cargador medido en ms ni cota de conexiones, pese a ser las pantallas nuevas más pesadas de la épica (7 lecturas en serie en `audit/page.tsx:64-80`, `readDataQuality` con tres `take: 5000`). CLAUDE.md §Estándar exige «medir (ms) en tests de rendimiento sobre el fixture completo». | Añadir los dos cargadores a `loaders[]` con su `MAX_MS` y la cota `MAX_CONNECTIONS`, como hicieron E6 y E8. |
| 4 | `lib/auth-db.ts:71` · `eslint.config.mjs:18` | **PUEDE** | `authPrisma` es un cliente sin tenant y **no está bajo `no-restricted-imports`** (la regla sólo cubre `prisma` de `@/lib/db`); `lib/auth-db.ts` figura además en la lista de exentos. Un `import { authPrisma }` en un modelo de negocio pasaría el lint. | Extender `no-restricted-imports` a `@/lib/auth-db` con lista blanca `models/users.ts` + `lib/auth.ts`. El daño está acotado por los `GRANT` de `app_auth` (da 42501, no vacío silencioso), por eso es PUEDE y no DEBE. |
| 5 | `prisma/migrations/20260917090000_e7_grupos_n_a_m/migration.sql:178` | **PUEDE** | El `CONSTRAINT TRIGGER` de I-E7-11 es `AFTER INSERT` **sólo**. Un grupo creado sin ninguna fila de pertenencia no se comprueba jamás (queda `Σ = 0 = 0` por vacuidad y vivo). Hoy es inalcanzable —`createMatchGroup` siempre inserta la estrella en la misma transacción—, pero la garantía se apoya en la aplicación, no en la base. | Un `CHECK` diferido de «todo grupo vivo tiene ≥ 1 miembro», o dejarlo escrito como límite conocido. |
| 6 | `tests/integration/e4-qa-attack.test.ts:321` | **PUEDE** | Falla por *timeout* de 5 s en la suite completa y **pasa aislada** (14 ✓ en 11,7 s): flaky por contención del pool, no regresión de E7 (el test no toca nada de la épica). | Subir el `testTimeout` de ese caso, como se hizo con el criterio 15 de E6. |

## Comprobaciones exigidas — resultado

- **(1) M4 `bigint`.** Borde correcto y exhaustivo: `centsFromDb/centsToDb` en `lib/money.ts:186-230` con `Number.isSafeInteger` y excepción; **todas** las lecturas cruzan el borde, `$queryRaw` incluidos (`ledger.ts:691,743`, `analytics.ts:804,1054`, `margins.ts:365`, `bank.ts:156,669`, `audit.ts:317-320`). `canonicalForm` rechaza `BigInt` y `string` en `hash.ts:157` (`centsToken`); `computeLedgerHash` usa `::text` en SQL, que es idéntico para `integer` y `bigint`. **Ningún `BigInt` se serializa a JSON.** Fixtures y hashes congelados **intactos** (ninguna línea borrada bajo `tests/fixtures/` ni `docs/design/fixtures/`). Ventana `ACCESS EXCLUSIVE` **medida y documentada** en la migración (24 ms fixture · 1,48 s a 500 k líneas).
- **(2) M5 RLS en `users`.** Políticas por rol correctas (`app_auth` `USING(true)`; `app_runtime` uno mismo + membresía de `app.current_org()`; `DELETE` `RESTRICTIVE`), `FORCE` activo y verificado (`e7-tenant.test.ts:366`). `app_auth` es `NOBYPASSRLS` con `GRANT` sobre cuatro tablas y **ninguna más**, con test que lo demuestra. `AUTH_DATABASE_URL` cae en `DATABASE_URL` **avisando por consola** (`auth-db.ts:50-58`) y está en `.env.example`, `dev-db-setup.sh` y la config RLS. **Sin escape.**
- **(3) Append-only.** `invariant_runs`: `REVOKE UPDATE, DELETE` + dos políticas `RESTRICTIVE`, con test de `42501`. Semi-append-only por **`GRANT` de columna** en `store_sweeps`, `bank_statements` (`line_count`), `bank_statement_lines` (5 columnas de ignorado), `bank_match_groups` (3 de desconciliación) y `bank_reconciliations` (`group_unmatched_at`), cada una con su política `no_delete`. La migración `20260917090000` no usa ninguna sentencia de SUPERUSER (sólo DDL propio) y es coherente con M3: rehace los dos índices únicos parciales sobre las filas ancla y mueve la igualdad de pareja a igualdad de grupo, que es lo que M3 impedía.
- **(4) Motor puro.** `lib/audit/**` y `lib/bank/**` sin `Date.now()`, `new Date()`, `prisma`, `fetch`, `Math.random` ni `process.env` (grep limpio).
- **(5) Revalidación en escritura.** Doble: en aplicación (`models/bank.ts:797-808`, `MATCH_NOT_BALANCED` con tolerancia 0) y en base (`bank_reconciliations_guard` para `SIMPLE` + `bank_match_groups_balanced` diferido al COMMIT). `matchToleranceDays` **no aparece en `invariants-e7.ts`**: sólo en `configHash` (`run.ts:102`) y en la sugerencia (`bank.ts:1045`), como fija D6.4.
- **(6) T23.** `lib/ledger/postFromProposal.ts` **sin tocar** (diff vacío). Asiento y conciliación en la **misma** `tx` (`bank-proposal.ts:128-160`), con `sourceType: BANK_RECONCILIATION` puesto por el llamante y la 57x localizada por cuenta, no por posición. Art. 20.Uno.18º aplicado como **exención con base**, no como «sin impuesto» (`lib/bank/proposal.ts:163`), y sellado en el `AuditLog`.
- **(7) Barrido.** En cola (`ai/store-sweep.ts`), lotes de 50, `sha256` en streaming, cota de 1 000 hallazgos, **una transacción corta por lote**, cancelación mirada entre lotes, `ConcurrencyLimiter(1)` por organización, y `promise.catch()` para que un fallo del trabajador no tumbe la petición. No bloquea.
- **(8) Rendimiento.** Agregados SQL en una pasada (`headlineFigures`, `computeLedgerHash`, I-E7-1); sin N+1 (`bank/page.tsx:42` abre **una** `tenantTransaction` y `audit/page.tsx` va por `tenantPage` con lecturas en serie; `listOrganizationMembersWithUsers` reentra en la misma transacción vía `withTenantGucs:759-762`). **Falta el test por cargador** → hallazgo 3.
- **(9) UX.** `SIN_EVALUAR` es estado propio con estilo punteado y nunca verde (`family-cards.tsx:37`, `families.ts:121-128`). Drill-down a `/ledger/{entryId}` y a los runs de liquidación desde la propia tarjeta (≤ 3 clics). Motivo obligatorio ≥ 10 caracteres en desconciliar, ignorar y forzar/levantar revisión (`forms/bank.ts:78,105`, `forms/audit.ts:61,68`). Estados vacío, carga (`audit/loading.tsx`) y error (`audit/error.tsx`, que cubre las rutas anidadas) presentes.
- **(10) Scripts.** `prune-runs.ts` y `migrate-cashflow-report-type.ts`: simulación por defecto, `--apply` explícito, `DATABASE_URL_MAINTENANCE`, marca **antes** del backfill, `NO FORCE` → DML → `FORCE` con test de que `report_runs` vuelve a `FORCE`, y `comprobarFicherosDeExtracto` impidiendo borrar un `File` de un `BankStatement` (O-22). Idempotentes y seguros.
- **(11) Tests.** Ninguno debilitado: los cambios en `e4-analytics`, `e6-reports`, `e8-actions` y `e8-t18` son adaptaciones mecánicas al borde `bigint` y al `ReportType` unificado, con la aserción **igual de fuerte**. **Cero `skip`/`todo` nuevos** (los 11 son heredados).
- **(12) Deuda.** Las cuatro entradas de `runs/registro.jsonl` declaran deuda fechada y con épica; la de la ola B (I2 en `detectionTest`) está **efectivamente cerrada** en la ola C. La única deuda **no declarada** es la documental del hallazgo 1.
- **(13) Los cuatro fallos que la UI destapó.** `scope` coherente: `CASHFLOW` unificado en acción, notas, etiquetas, `SUMMARY_TYPES`, `buildReport` y el selector de revisión manual, conservando los dos tipos viejos para los runs históricos, y con `migrate-cashflow-report-type.ts` actualizando también `manual_review_flags.scope` — **con test** (`e6-reports.test.ts`, `e7-retencion.test.ts`). Export desde `"use server"`: `MAX_STATEMENT_BYTES` pasa a constante **no exportada** con la razón escrita (`actions.ts:365-369`) y el `build` en verde lo demuestra. `ANALYTIC_DEST`: `projectId`/`costCenterId` entran en `ProposeFromLineInput` y los elige una persona, no el motor (`bank-proposal.ts:63-75`). Bloque de informes en `detectionTest`: se arma con **las mismas líneas** que ya tiene el motor, de modo que la alteración se propaga a los dos bloques, e `I1` sale en `detectedBy` con test que además comprueba que el `ledgerHash` no se mueve. Las cuatro correcciones son correctas y tienen prueba.

## Veredicto

**CAMBIOS REQUERIDOS** — 0 BLOQUEA · 3 DEBE · 3 PUEDE.

El núcleo de la épica está bien: el borde `bigint` es hermético y los hashes no se mueven, la RLS de `users` protege sin escape y sin `BYPASSRLS`, el grupo N-a-M cuadra en la base **y** en el barrido con tolerancia 0, el motor es puro y `postFromProposal` no se ha tocado. Lo que falta no es contable sino de cierre: **la documentación que el propio ADR-0015 se comprometió a actualizar no existe** —y con ella los diecisiete invariantes nuevos, que hoy sólo están en código, contra el §Principio 4 de `CLAUDE.md`—, el criterio 1 y 2 de «explicado» de D2 quedó sin conectar, y las dos pantallas nuevas entran sin test de rendimiento. Los tres se cierran en E7, no en otra épica.

---

# Ronda 2 — verificación de cierre (`f227f1a`, `git diff 34f2802...f227f1a`)

**Suites (todas ejecutadas de nuevo):** `lint` **0 errores** · `test` **1 543 ✓ / 11 skip** · `test:integration` **2 038 ✓ / 0 fallos** (107 ficheros; el flaky de la ronda 1 ya no aparece) · `test:integration:rls` **185 ✓** · `build` **OK**. e2e no ejecutado (por encargo).

| # ronda 1 | Cierre | Evidencia |
|---|---|---|
| **DEBE 1** (docs) | **CERRADO** | `SKILL.md` **23** ocurrencias de `I-E7-`, con la regla del badge P6 por composición (l. 157-159) y los motivos de sello nuevos (l. 153); `MODELO-DATOS.md` 6 (l. 206 ya con `CASHFLOW` unificado, l. 257 `bank_pending_kinds`, l. 268-273 el bloque `bigint`); `ARQUITECTURA.md` 5. `ESTADO.md` reescrito con la ronda y sin deuda diferida (l. 249). |
| **DEBE 2** (`resolvedLaterIds`) | **CERRADO, y mejor que lo pedido** | Ya no es un `Set()` vacío: `invariants-e7.ts:547` lo **deriva del propio cuadre** y `confidence.ts:166` lo une al de entrada; `bank/shared.ts:70` lo propaga a los dos lados. Además la causa raíz (H-5): el tipado no tenía dónde escribirse y los doce pendientes salían `kind: null` — ahora hay tabla, acción y limpieza. |
| **DEBE 3** (rendimiento) | **CERRADO** | `perf-audit.test.ts` nuevo con **cinco techos** en ms (`MAX_MS_AUDIT` 500, `SWEEP` 3 000, `BANK_PANEL` 800, `SUGGESTIONS` 700, `REGISTRO` 300) y `MAX_CONNECTIONS 2`; `perf-pages.test.ts:317,342` añade `/audit` y `/audit/bank` a `loaders[]`. |
| **PUEDE 4** (`authPrisma`) | **CERRADO** | `eslint.config.mjs`: `authPrisma` entra en `no-restricted-imports` por nombre **y** por patrón, con lista blanca `lib/auth.ts` + `models/users.ts`. |
| **PUEDE 5** (grupo vacío) | **CERRADO EN LA BASE** | `app.bank_match_groups_has_members()` + `CONSTRAINT TRIGGER … DEFERRABLE INITIALLY DEFERRED` sobre `bank_match_groups` (`AFTER INSERT OR UPDATE`): un grupo vivo sin pertenencias aborta al COMMIT en vez de cuadrar por vacuidad. |
| **PUEDE 6** (flaky) | **CERRADO** | Timeout a `120_000` en los dos casos con el motivo escrito, **sin relajar una sola aserción**; y `e4-analytics` se vuelve **más estricto**: `findFirstOrThrow` sin `orderBy` pasa a excluir asientos anulados y contra-asientos con orden determinista, que era la causa real. |

**Lo nuevo, mirado con lupa.** La migración `20260918090000` es DDL sobre esquema propio más `app.enforce_tenant_rls('bank_pending_kinds')`: **no exige SUPERUSER**. `bank_pending_kinds` nace con RLS estricta, FK **compuestas por tenant** (con `CASCADE` razonado para no repetir BUG-E7-1), `CHECK num_nonnulls(...) = 1` y dos índices únicos parciales que impiden acumular tipados; está en `TENANT_MODELS` (`lib/db.ts`), en la lista de delegados de ESLint y en los tests de RLS (`e7-tenant.test.ts:42,176`). **No es append-only a propósito y está bien**: un tipado no es un hecho contable sino una anotación sobre algo que aún no cuadra, lleva autor y fecha, y `clearPendingKinds` lo borra al conciliar o ignorar, de modo que ningún tipado caducado puede sostener el badge. `typePendingAction` es `Role.EDITOR`, valida con `zod` sobre enum cerrado, comprueba que lo tipado siga pendiente y escribe `AuditLog` con `before`/`after`. `app.bank_line_amount_in_currency` es la pieza clave de H-1 y está bien planteada: `IMMUTABLE`, devuelve **`NULL` en vez de aproximar** cuando el apunte no lleva su importe en la divisa de la cuenta, y los dos guardias lo delatan con `23514` —el del grupo cuenta además los `NULL` por separado—; se corrige **en la base**, que es donde M3 puso la barrera. `readFxCloses` es **un solo agregado** con `unnest` para todas las cuentas en divisa (sin N+1), excluye `CLOSING`, cruza el borde con `centsFromDb` y deja `rateMicro` como `bigint` que sólo consume `convertWithRateMicro` (aritmética entera, half-even): **ningún `BigInt` llega a JSON ni al hash**. H-2 (`entry_kind <> 'CLOSING'` en la consulta y en los cuatro `CASE`), H-4 (los motivos de E7 entran en `seal()` por `auditReasons`, restaurando «sin razones ⇔ VALIDADO AUTOMÁTICAMENTE»), H-6 (segunda lectura acotada a `OPENING` posterior al corte, que no mueve I-E7-15/16/17) y H-7 (`periodDeclared` del registro 11 y de la cabecera CSV) están cerrados con test. **Motor sigue puro** (`lib/audit/**`, `lib/bank/**` sin `Date.now`, `prisma`, `fetch` ni `Math.random`), **cero `skip`/`todo` nuevos** y ningún test debilitado.

| # | Fichero:línea | Severidad | Problema | Sugerencia |
|---|---|---|---|---|
| R2-1 | `models/audit.ts` (comentario de `headlineFigures`) | **PUEDE** | El comentario de la corrección cita «el hallazgo **H-1** (ALTA) de la auditoría», pero en `E7-auditoria-informe.md:122` ese hallazgo es **H-2** (H-1 es el cuadre en divisa). Errata de referencia cruzada, no de código. | Cambiar `H-1` por `H-2` en ese bloque. |

## Veredicto ronda 2

**APROBADO** — 0 BLOQUEA · 0 DEBE · 1 PUEDE (errata de referencia en un comentario).

Los tres DEBE y los tres PUEDE de la ronda 1 están cerrados con evidencia verificable, y dos de ellos por encima de lo pedido: el DEBE 2 no se limitó a poblar el conjunto sino que construyó el camino que faltaba para tiparlo, y el PUEDE 5 se resolvió en la base en vez de en la aplicación. Los siete hallazgos del auditor y los dos bugs de QA tienen corrección y test. Las cinco suites están en verde, incluido el caso que en la ronda 1 fallaba por contención del pool. R2-1 es una errata de comentario y no bloquea el merge.
