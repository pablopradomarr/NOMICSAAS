# E11 — Revisión de código (contexto limpio)

**Diff:** `git diff 366c347...HEAD` — 4358e75 (ola B), 4a43350 (ola A), 75a2237 (ola C),
18b3326 (integración, D9 modo INTERNO). 124 ficheros, +19 376 / −1 052.
**Revisor:** `revisor-codigo` · **Fecha:** 2026-09-15

## Verificación ejecutada

| Comando | Resultado |
|---|---|
| `npm run lint` | **VERDE** — 0 errores, 12 avisos preexistentes |
| `npm run test` | **VERDE** — 117 ficheros, 2 463 pasan, 11 `skip` (todos `skipIf` de base, ninguno nuevo sin motivo) |
| `npm run test:integration` | **ROJO** — 5 fallos / 3 281 (ver #3 y #15) |
| `npm run test:integration:rls` | **VERDE** — 12 ficheros, 211 pasan |
| `npm run build` | **VERDE** |

## Hallazgos

| # | Fichero:línea | Severidad | Problema | Sugerencia |
|---|---|---|---|---|
| 1 | `lib/ledger/invariants-e11.ts` (no existe) | **BLOQUEA** | **T20 sin hacer.** No hay `I-E11-1…13` ejecutables: ninguna implementación, ningún bloque `platform` en `runLedgerInvariants` (`models/ledger.ts:2220`), ninguna familia `PLATAFORMA` en el motor, y `.claude/skills/fiabilidad/SKILL.md` no menciona I-E11. Los trece sólo existen como prosa en comentarios (`models/usage.ts:13`, `lib/platform/subscription.ts:6`, `models/backups.ts:18`…). Es **H-1 de E9 y H-1 de E10 por tercera vez**: invariantes prometidos que nadie ejecuta. §16 programa T20 tras las tres olas, así que no es una regresión, pero **E11 no se cierra sin él** | Implementar T20 completo (los trece, el bloque `platform`, la familia, los motivos de sello y los **tres tests estáticos de AST**) antes de dar E11 por cerrada |
| 2 | `app/(app)/settings/members/actions.ts:48`, `app/(auth)/invite/[token]/actions.ts:160`, `app/(app)/organizations/actions.ts:39` | **BLOQUEA** | **T13 incompleto: 3 de las 7 acciones de cuota dura no llaman a `assertWithinLimit`.** Sólo están cableadas `maxOcrDocsMonth`, `maxStorageBytes`, `maxExportsMonth` y `maxBackupsMonth`. **`maxMembers` y `maxOrganizations` no se aplican en ningún sitio del servidor** — `grep maxMembers` fuera de `lib/platform/` y `models/plans.ts` no devuelve ni una comprobación. `models/platform-limits.ts:19` documenta las siete como si estuvieran cableadas, y §3.5 del diseño las declara «lista exhaustiva» | Cablear las tres dentro de su transacción de escritura (`delta = 1`; `acceptInvitationAction` revalida). I-E11-4c fallará en cuanto se escriba |
| 3 | `tests/integration/e11-integracion-d9.test.ts:97`, `tests/integration/e11a-esquema.test.ts:277` | **BLOQUEA** | **I-E11-5 FALLA en la suite completa** (`'1'`/`'2'` organizaciones sin `Subscription`, esperado `'0'`). Aisladas las dos suites pasan: la aserción cuenta huérfanas **en toda la base**, y una docena de ficheros de E1…E10 crean organizaciones con `INSERT` directo. Una aserción de alcance global sobre una base compartida deja la suite permanentemente roja | Acotar la consulta a las organizaciones que crea el propio test (o a `created_at >= el inicio del fichero`), y dejar el barrido global para el I-E11-5 real de T20 |
| 4 | `models/subscriptions.ts:382-387` | **BLOQUEA** | `ensureSubscriptionForOrganization` **traga el error de `getPlanAt` con un `console.warn` y retorna sin crear la fila** («la organización nace sin fila»). Produce exactamente el estado que I-E11-5 prohíbe y que D9 promete imposible («toda organización nace con el plan `ILIMITADO`»), en silencio y en el camino de alta | Lanzar. Si no hay versión vigente del plan por defecto, el alta debe abortar la transacción entera, no nacer coja |
| 5 | `app/(app)/settings/backups/page.tsx:123-140` | **DEBE** | `readChecks` espera `{ key, label, ok, detail }`, pero `verifyRestore` produce `CheckResult = { id, status, title, evidence, note }` (`lib/platform/backup.ts:322`). El `if (typeof entry.key !== "string") return []` descarta **todas** las comprobaciones: **las seis verificaciones nunca se pintan**, ni en `DONE` ni en `DONE_UNVERIFIED`. Contradice D2.7 («con las seis comprobaciones enfrentadas a la vista») y T22 | Leer `id`/`status`/`title`/`evidence` y pintar la evidencia enfrentada origen↔destino. Añadir un test de render sobre un `restoreVerification.json` real |
| 6 | `lib/storage/keys.ts:113` | **DEBE** | **`assertKeyBelongsTo` no se invoca en ningún punto de producción** — sólo en `lib/storage/keys.test.ts`. Su docstring afirma «Toda lectura y toda escritura del driver pasa por aquí (…) es una excepción hoy». Hoy el aislamiento lo da sólo RLS sobre `stored_objects`; la segunda barrera que el fichero documenta no existe | Llamarla en `models/storage.ts` (`putObject`, `getObjectBuffer`, `deleteObject`) y en `lib/documents.ts`, o retirar la promesa del comentario. Preferible lo primero |
| 7 | `tests/integration/perf-platform.test.ts` (no existe) | **DEBE** | **Ninguno de los diez techos de §12 está medido** (T26). El techo 5 («memoria estable», pico medido) es el que justifica la deuda del ZIP en memoria: hoy la deuda está fechada pero el techo que la acota no se mide | Escribir T26 con el volumen sembrado en el propio test, antes de cerrar |
| 8 | `models/backups.ts:944-952` | **DEBE** | La comprobación `NUMERACION` compara `actual.gaps.length === expected.gaps.length` y lo mismo con duplicados: compara **recuentos contra el origen**, no la ausencia de huecos, ni **qué** huecos. Un origen con dos huecos y un destino con dos huecos distintos da PASS, y D2.6.2 pide «sin huecos ni duplicados» | Comparar los arrays completos y, además, exigir `gaps.length === 0` cuando el origen no los tenía |
| 9 | `lib/files.ts:164` | PUEDE | `isEnoughStorageToUploadFile` se queda sin llamantes tras T13: código muerto (CLAUDE.md lo prohíbe) y segundo criterio de cuota divergente del derivado | Retirarla junto a `getOrganizationStorageUsed` si tampoco se usa |
| 10 | `models/platform-limits.ts:250-262` | PUEDE | Rama muerta: `to_regclass('public.platform_audit_logs')` es andamio de la ola B, M3 ya la creó. Además la excepción automática se registra con `actor: 'cron'` aunque la dispare una acción de usuario | Quitar el `to_regclass` y pasar el actor real |
| 11 | `lib/storage/s3.ts:220-228` | PUEDE | `head()` devuelve el `sha256` que publica **el propio almacén** (`x-amz-meta-sha256`), y `verifyObject` lo compara contra la fila. Una alteración que reescriba también el metadato pasa I-E11-6 sin descargar un byte | Documentar el límite en el enunciado de I-E11-6 y añadir un modo de barrido profundo (descarga y recómputo) para una muestra |
| 12 | `…m4_backfill_suscripciones/migration.sql:54-80`, `…m6_plan_ilimitado_bigint/migration.sql:143-168` | PUEDE | La guardia O-14 de `ai_balance` admite puerta de escape por `app.e11_ai_balance_resuelto`. Queda traza en `platform_audit_logs`, pero permite migrar con un pasivo prepagado vivo | Aceptable si el runbook lo exige con firma del operador; hoy `docs/deploy/e11-plataforma.md` no lo documenta |
| 13 | `…m3_plataforma/migration.sql:149` | PUEDE | `platform_audit_logs_read … FOR SELECT USING (true)` y la tabla lleva `organization_id`: cualquier lectura con `app_runtime` ve las líneas de todas las organizaciones | Política por `organization_id IS NULL OR organization_id = app.current_org()`, o dejar la tabla fuera de todo camino de tenant y decirlo |
| 14 | `models/onboarding.ts:612-644` | PUEDE | `deleteDemoOrganization` borra por `onDelete: Cascade` una organización **con asientos**. Está acotado a `is_demo = true`, con `AuditLog` y motivo, y O-6 lo autoriza — pero ADR-0019 D2.9 dice «no existe borrado de organización con asientos (…) **y un test lo comprueba**», y ese test no está | Añadir el test de D2.9: el borrado rebota en una organización sin `is_demo` que tenga asientos |
| 15 | `tests/integration/e10-ronda1.test.ts:861,917` · `tests/integration/perf-budget.test.ts:462` | PUEDE | Tres de los cinco fallos de integración **no son de E11**: los dos de E10 son dependientes de orden (`more than one row returned by a subquery`, pasan aislados) y el de perf es un margen (331 ms sobre 300) bajo carga concurrente | Anotarlos como deuda de test de E10; no bloquean este diff |

## Lo verificado y conforme

- **Migraciones M1…M6**: ninguna exige SUPERUSER; enums en su propia migración y **ningún `ALTER TYPE … ADD VALUE`**; el único `ALTER FUNCTION … OWNER TO app_maintenance` (`M5:282`) repite el patrón aprobado en `20260906090000_e3_rls_helpers`; **baile `NO FORCE → backfill → FORCE`** en M4, M5 y M6 con bloque de verificación «ninguna tabla queda en NO FORCE» en las cinco; `bigint` en `max_storage_bytes`, `storage_bytes`, `size_bytes`, `fx_rate_micro`, `storage_used`/`storage_limit`; **ninguna migración aplicada editada** (`git diff --name-status` sólo devuelve `A`); guardia O-14 presente y verificada; `enforce_tenant_rls` + `TENANT_MODELS` para cada tabla de tenant nueva.
- **Seguridad**: webhook con firma verificada por `constructEvent`, idempotencia por el `UNIQUE` de `subscription_events.stripe_event_id`, `200` por defecto, `ORPHAN_WEBHOOK` sin dar de alta tenants, único `4xx` la firma; las **tres** rutas `/api/stripe/*` responden `404` en modo `none` antes de tocar cliente, sesión o base (`app/api/stripe/disabled.ts`); cron con `timingSafeEqual` y buffer de longitud igualada, cubo consumido también en el `401`, sin distinguir job inexistente, `refDate` explícito persistido y **la ocurrencia fechada por su periodo de devengo** (`models/cron-jobs.ts:241`), `202` con cursor en `PARTIAL`; `/api/health` sin PII y `503` sólo si la base cae; ningún secreto en `console.*`; driver S3 con **endpoint fijo por configuración** (sin SSRF), SigV4 firmado a mano con `x-amz-content-sha256` y sha comprobado **antes** de enviar, `signedUrl` con `X-Amz-Expires` y query canónica ordenada; rate limit **persistente** en `rate_limit_buckets` con la clave siempre `sha256` (CHECK en base); `platform_audit_logs` append-only por `RESTRICTIVE` + `REVOKE UPDATE, DELETE`.
- **Backup/restore**: inventario **derivado** de `TENANT_MODELS` (`backupInventory`) y **I-E11-7 probado contra el esquema real** (`tests/integration/e11-olab.test.ts:125`), no contra una lista a mano; manifest firmado HMAC-SHA256 con `keyId` rotable y **`signingKeyFromEnv` lanza si la clave está vacía**; restauración siempre a organización nueva, dentro de una transacción con **aborto total por una sola fila** (`RestoreAbort` con tabla, línea y motivo); reasignación de ids limitada a `id` y a las FK **leídas de `pg_constraint`**; `invitations.token_hash` **reemitido** con justificación correcta (un mismo enlace no puede abrir dos organizaciones); `global/exchange_rates.jsonl` con sha verificado y fallo si una tasa existe con otro valor; `DONE` reservado a `verified`. **El re-sellado de `entry_hash` no rompe P7**: el sello lleva uuid por diseño (ADR-0011), no es comparable entre organizaciones, y lo que se comprueba —cada fila contra su propio recálculo, más el `ledgerHash` id-independiente del check 5, los recuentos `=` y la numeración— es el criterio correcto; los sellos que **sí** se comparan byte a byte son los derivados del contenido (`derivedSealColumns()`), con `journal_entries.entry_hash` explícitamente excluido de esa comparación. ZIP en memoria: **deuda fechada en E12** en `docs/ESTADO.md` con motivo.
- **Uso y cuotas**: **ningún contador se incrementa al escribir** — las seis métricas son agregados SQL cacheados por `sourceHash` (recuento + `maxUpdatedAt`, que sí baja al anular); `assertWithinLimit` corre **dentro de la transacción de la escritura** en las cuatro acciones cableadas; `postEntryAction` y la generación de recurrentes **no la invocan** (O-3 respetada); dos clases de cuota sostenidas por el **tipo** (`checkLimit` sólo acepta `HardLimitKey`); `maxStorageBytes` dura en `FULL` y blanda fuera (O-16); `backupConsumesQuota` deja `EXIT`, `SCHEDULED` y todo lo que no sea `FULL` sin cuota (O-4); **`-1` resuelto antes de mirar el uso en las tres comparaciones** (`checkLimit`, `checkSoftEntries`, `usage-bars.tsx:52`); modo INTERNO `FULL` siempre salvo desactivación del propio ADMIN, cableado en `lib/authz.ts:159`, `models/subscriptions.ts:139` y `lib/platform/subscription.ts:163`.
- **Onboarding**: siembra de las **nueve** piezas dentro de la transacción del alta (`createOrganizationWithOwner({ seed })`), `isDemo` marcado antes de la primera fila e **inmutable por trigger** (`…onboarding_demo_preferencias:69`), demo en organización propia que no cuenta contra `maxOrganizations` ni contra el uso.
- **Lectores de ficheros**: los seis pasan por `lib/documents.ts`. El `fs` residual es de previsualizaciones regenerables (`lib/previews/*`), `static/` (logo y avatar) y `ai/attachments.ts` sobre miniaturas — **ninguno lee el justificante original**, y las dos excepciones están fechadas en E12 en `ESTADO.md`.
- **UX**: `VIEWER` ve uso pero no facturación ni botones (`settings/subscription/page.tsx:38`, `settings/backups/page.tsx:27` + `minRole: ADMIN`), estados vacío/carga/error presentes, y **cero aritmética contable en cliente** (lo único que se calcula es el % de una barra y el formato de bytes).
- **Documentación**: `.env.example` y `docs/deploy/e11-plataforma.md` traen las variables nuevas con **defaults seguros** (`BILLING_PROVIDER="none"`, `STORAGE_BACKEND="local"`, `CRON_SECRET=""` ⇒ ruta cerrada, `PLATFORM_SIGNING_KEY=""` ⇒ el backup no se emite). Toda la deuda que la ronda deja abierta está **fechada en E12** con motivo.

## Veredicto

**CAMBIOS REQUERIDOS**, y **BLOQUEADO para el cierre de E11** mientras T20 no exista.

Las tres olas están bien construidas y la integración D9 es sólida: las migraciones cumplen
las reglas de RLS y de rol no superusuario al pie de la letra, el backup 2.0 cierra G-15 de
verdad y el razonamiento sobre el re-sellado de `entry_hash` es correcto. Lo que impide
aprobar son cuatro cosas concretas: **T20 no está** (los trece invariantes son prosa, que es
el fallo que E9 y E10 ya pagaron dos veces), **dos de las siete cuotas duras no se aplican en
servidor**, **`npm run test:integration` está rojo en HEAD** con dos fallos propios de E11, y
**el alta puede crear una organización sin suscripción en silencio**. Los puntos 5 a 8 deben
caer en la misma ronda; 9 a 15, antes de cerrar la épica.

---

# Ronda 2 — `git diff 18b3326...1ef4692`

**Commit:** 1ef4692 · 41 ficheros, +4 040 / −161 · **Fecha:** 2026-09-15

## Suites

| Comando | Resultado |
|---|---|
| `npm run lint` | **VERDE** — 0 errores, 12 avisos preexistentes |
| `npm run test` | **VERDE** — 118 ficheros, 2 526 pasan (+63), 11 `skip`, todos `skipIf` de base |
| `npm run test:integration` | **1 fallo / 3 372** (172 ficheros) — sólo `perf-platform` 3/10, ver R2-1. Los cinco fallos de la ronda 1 **han desaparecido**, incluido el flake de `perf-budget` 2/9 |
| `npm run test:integration:rls` | **VERDE** — 211 pasan |
| `npm run build` | **VERDE** |

## Cierre de los hallazgos de la ronda 1

| # (R1) | Estado | Evidencia |
|---|---|---|
| 1 · T20 ausente | **CERRADO** | `lib/ledger/invariants-e11.ts` (51 KB, `E11_INVARIANT_IDS` con los trece), `models/platform-invariants.ts` (lector), bloque `platform` en `models/ledger.ts:2246-2312` con sus `platformReasons` en el sello, `runPlatformInvariants` invocado en `lib/ledger/invariants.ts:596`, familia `PLATAFORMA` en `lib/audit/families.ts:39` + prefijo `["I-E11-", "PLATAFORMA"]`, migración del enum `check_family`, 16 menciones en `SKILL.md`, `lib/ledger/invariants-e11.test.ts` (674 líneas) |
| 2 · 3 de 7 cuotas sin guardián | **CERRADO** | Las **siete** cableadas: `settings/members/actions.ts:76`, `invite/[token]/actions.ts:221`, `unsorted/actions.ts:201`, `files/actions.ts:35`, `reports/actions.ts:233`, `models/backups.ts:1248`, `organizations/actions.ts:90` |
| 3 · I-E11-5 global rompía la suite | **CERRADO** | Acotado en `e11-integracion-d9.test.ts` y `e11a-esquema.test.ts`; la suite completa ya no lo falla |
| 4 · `ensureSubscription` tragaba el error | **CERRADO** | `models/subscriptions.ts:395-405` — `getPlanAt` sin `try/catch`: el alta aborta entera |
| 5 · `readChecks` no pintaba las seis | **CERRADO** | `settings/backups/page.tsx:133-160` lee `{id,status,title,evidence,note}` y trae la evidencia enfrentada; `INFO` no cuenta como ✓ |
| 6 · `assertKeyBelongsTo` sin llamantes | **CERRADO** | `models/storage.ts:55, 93, 155` (put, get, delete) |
| 7 · techos de §12 sin medir | **PARCIAL** | `tests/integration/perf-platform.test.ts` existe con los diez, pero el 3 **falla** (R2-1) y se mide sin `extraction_runs` (R2-3) |
| 8 · `NUMERACION` comparaba recuentos | **CERRADO** | `models/backups.ts` — compara los arrays completos y exige cero huecos si el origen no los tenía |
| 9 · `isEnoughStorageToUploadFile` muerta | **RETIRADO — era mío** | Sí tiene llamante: `lib/uploads.ts:157`, y sólo en `uploadStaticImage` (logo/avatar), así que **no** choca con la cuota blanda de O-16 |
| 10 · `to_regclass` / actor | **CERRADO** | `models/platform-limits.ts:396` retira la rama y corrige el actor |
| 11 · `head()` confía en el metadato | **CERRADO** | Límite declarado en el enunciado de I-E11-6 |
| 12 · puerta de escape de O-14 | **CERRADO** | Documentada en el runbook |
| 13 · `platform_audit_logs` legible por todos | **CERRADO** | Migración `…091000_e11_platform_audit_logs_por_tenant`: `organization_id IS NULL OR = app.current_org()`, con verificación de `FORCE` y de las cuatro políticas |
| 14 · falta el test de D2.9 | **CERRADO** | `tests/integration/e11-ronda1.test.ts:328` |
| 15 · fallos de E10 dependientes de orden | **CERRADO** | Ya no se reproducen en la suite completa |

Cerrados también los del auditor y QA que se me pidió verificar: **H-2** (`BACKUP_TENANT_MODELS` en `lib/db.ts:280` incluye `TENANT_MODELS_WITH_GLOBAL`, así que `currencies` y sus 177 filas viajan en el ZIP), **H-3** (`isDemo` en el `INSERT`), **H-4** (`getSubscription` filtra por `WHERE organization_id = $1`, no sólo por RLS), **H-5** (`checkSoftEntries` cableado en `models/ledger.ts:1197` **después** de postear y sin lanzar nunca), **H-6** (comprobación 6 relativa por `manifest.sourceSweep`, opcional y con el criterio absoluto declarado en la evidencia cuando falta) y **BUG-E11-2** (`E11_RESET_TABLES` + `RESET_ORG_PRESERVED` con motivo escrito y derivables del esquema).

## Lupa de esta ronda

- **Exclusiones del inventario: justificadas, documentadas y exigidas.** `PLATFORM_ONLY_TABLES` (`lib/db.ts:311-345`) declara `platform_audit_logs`, `platform_invoices`, `subscriptions` y `subscription_events` **con motivo escrito**, y los motivos son correctos: serie correlativa global nuestra (art. 28.2 CCom), `organization_id` UNIQUE, `stripe_event_id` UNIQUE global y filas sin organización. **I-E11-7 es fuerte de verdad**: falla si aparece cualquier tabla con `organization_id` fuera del inventario y de la lista, falla si una exclusión declarada ya no existe, enfrenta inventario y manifest en las dos direcciones y publica los motivos en el mensaje de PASS. Es la forma correcta de no repetir BUG-E7-1/E9-5/E10-1 por quinta vez.
- **`PLATAFORMA` fuera de la puerta del cierre: coherente con D7.** `models/closing.ts:1466-1500` filtra `I-E11-` de `INVARIANTES_PASS` **y** del recuento de runs en `REQUIERE_REVISION`, con el razonamiento correcto: cerrar el ejercicio es el hecho contable por excelencia y ningún asunto de plataforma puede impedirlo. La familia conserva su tarjeta y su semáforo en `/audit` y sigue sellando el periodo con su motivo, que es donde debe pesar.
- **Migraciones nuevas: sin SUPERUSER.** `ALTER TYPE check_family ADD VALUE IF NOT EXISTS 'PLATAFORMA'` va **sola** en su migración (precedente de `…e10_check_family_presupuesto`) y `DROP/CREATE POLICY` lo puede el propietario. Ninguna mueve filas, ninguna deja nada en `NO FORCE`, ninguna migración aplicada editada.
- **Tests no debilitados.** +63 unitarios, +3 ficheros de integración (`e11-ronda1`, `e11-qa`, `perf-platform`), ningún `skip`/`todo`/`fixme` nuevo y ninguna aserción relajada, salvo las dos que R2-1 y R2-3 señalan.

## Hallazgos nuevos

| # | Fichero:línea | Severidad | Problema | Sugerencia |
|---|---|---|---|---|
| R2-1 | `tests/integration/perf-platform.test.ts:421` | **DEBE** | **El techo 3 de §12 se incumple y el test lo dice**: `medido 592 ms @ 5 000 y 4 032 ms @ 10 000 · extrapolado a 50 000 = 34 405 ms` contra un techo de **1 200 ms**. Peor que el número: duplicar los asientos multiplica el tiempo por **6,8**, así que `computeUsage` recalculando **no escala linealmente** y la propia extrapolación lineal es optimista. No es un flake: es el agregado del uso creciendo de forma superlineal | Perfilar los seis agregados (probable falta de índice por `(organization_id, entry_date)` en el recuento de `entries`, o un plan que pasa a *seq scan*). **No subir el techo** |
| R2-2 | `lib/ledger/invariants-e11.ts` (≈ byte 3 736) | **DEBE** | El fichero contiene **dos bytes NUL literales** (el separador de `sortedSet(a).join(…)` escrito con el carácter crudo en vez de su secuencia de escape), así que **git lo trata como binario**: `git diff --numstat` devuelve `-` / `-` y el diff dice `Bin 0 -> 51375 bytes`. **El fichero central de T20 —los trece invariantes— no se puede revisar en un diff**; he tenido que leerlo con Python | Escribir el separador con una secuencia de escape (o usar el separador de unidad `U+001F`). Es un cambio de un carácter que devuelve 1 000 líneas a la revisión |
| R2-3 | `tests/integration/perf-platform.test.ts` (techo 3) | **DEBE** | El techo 3 **se degrada en silencio**: `[perf-platform] no se han podido sembrar extraction_runs: el techo 3 se mide sin esa dimensión`, y el test sigue adelante. Una de las tres dimensiones de §12 (5 000 documentos) no se mide, y el aviso va a `stderr`, donde nadie lo lee | Que la siembra fallida **falle el test**, o que el techo declare por escrito que mide dos dimensiones de tres |
| R2-4 | `models/platform-limits.ts:456-495` · `models/ledger.ts:1196` | PUEDE | El comentario promete que con `softMaxEntriesMonth = -1` «sale sin tocar la base, así que el camino caliente no paga nada», pero **siempre hace un `SELECT subscriptions JOIN plans` por cada `postEntry`**, dentro de su transacción. Es barato, pero el comentario no es exacto y el techo 4 de §12 lo paga. Además `postEntries` (lote) **no** lo invoca: un cierre o una importación masiva no producen el aviso del 80 % | Cachear el `soft` por organización en el contexto del ledger, corregir el comentario y decidir por escrito si el lote avisa |
| R2-5 | `models/platform-limits.ts:487` | PUEDE | El recuento de la cuota blanda excluye contra-asientos y asientos de sistema, pero **no `isDemo`**, que D1.6 y O-6 sí excluyen del uso. Hoy es inocuo (en INTERNO todos los techos son `-1`), pero diverge del criterio único de `lib/platform/usage.ts` | Reutilizar la exclusión de `usage.ts` en vez de reescribir el `WHERE` |
| R2-6 | `prisma/migrations/` | PUEDE | `20260928090000_e11_check_family_plataforma` y `20260928090000_e11_m6_plan_ilimitado_bigint` comparten **el mismo sello temporal**. Hoy el orden es determinista y correcto (`c` < `m` lexicográficamente), pero la convención `NNNN_<que_hace>` existe para que el orden lo fije el sello, no el alfabeto | Renombrar la nueva a `20260928092000_…` mientras ninguna esté aplicada en producción |
| R2-7 | `lib/db.ts:311` | PUEDE | `PLATFORM_ONLY_TABLES` exige el campo `reason` por tipo, pero nada impide que sea la cadena vacía: I-E11-7 imprimiría `tabla ()` y pasaría | Un `reason.trim().length > 0` en `checkIE117`, que es donde ya se lee la lista |

## Veredicto Ronda 2

**CAMBIOS REQUERIDOS**, pero de otra clase. **Los cuatro BLOQUEA de la ronda 1 están cerrados con evidencia**, y los ocho DEBE/PUEDE también (el 9 era un error mío y lo retiro). T20 no es un cumplimiento formal: los trece invariantes están implementados, leídos de la base por `models/platform-invariants.ts`, ejecutados en `runLedgerInvariants`, sellando el periodo, con familia propia en `/audit` y en `SKILL.md`; I-E11-7 quedó **más fuerte** de lo que pedía el enunciado, y excluir `PLATAFORMA` de la puerta del cierre es la lectura correcta de D7. **No queda ningún BLOQUEA.**

Lo que impide aprobar son tres cosas acotadas, y ninguna es de diseño: **el techo 3 de §12 se incumple por un factor de 29, con crecimiento superlineal del uso** (R2-1), **el fichero central de T20 es binario para git y por tanto no revisable en diff** (R2-2), y **ese mismo techo se degrada en silencio saltándose una de sus tres dimensiones** (R2-3). R2-4 a R2-7, antes de cerrar la épica.
